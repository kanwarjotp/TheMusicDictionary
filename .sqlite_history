.databases
.quit
.tables
.databases
# sid = request.form.get('studIdVal')
  sName = request.form.get('sName')
  lName = request.form.get('lName')
  email = request.form.get('email')
  psswd = request.form.get('psswd')
  
  con = sqlite3.connect('db1')
  con.row_factory = sqlite3.Row
  
  cur = con.cursor()
  cmd = "INSERT INTO students (name, last_name, email, psswd) VALUES('{0}', '{1}', '{2}','{3}')".format( sName, lName, email, psswd)
  cur.execute(cmd)
  
  con.commit()
  con.close()
;
CREATE TABLE users (id integer primary key autoincrement,  username varchar(30) not null, mail varchar(30) not null, password varchar(60) not null);l
;
CREATE TABLE users (id integer primary key autoincrement,  username varchar(30) not null, mail varchar(30) not null, password varchar(60) not null);
.tables
select * from users
;
select * from users
;
select * from users
;
.quit
